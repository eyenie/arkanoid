#include "GameObjContactListener.h"
#include "GameObj.h"

#include <iostream>

void GameObjContactListener::BeginContact(b2Contact* contact) 
{
/*     std::vector<GameObj *> objects;
 *     objects.push_back(static_cast<GameObj *>(contact->GetFixtureA()->GetBody()->GetUserData()));
 *     objects.push_back(static_cast<GameObj *>(contact->GetFixtureA()->GetBody()->GetUserData()));
 * 
 *     for (GameObj *o : objects) {
 *         if (o->type() == GameObj::BRICK) {
 *             o->setState(GameObj::DESTROYED);
 *         }
 *     }
 */
}
